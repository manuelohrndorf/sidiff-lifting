



===================================================================================


ResourceSet getModel()


List<Version> getRevisions()
List<Version> getSuccessorRevisions(Version v)
SymmetricDifference getLowLevelDifference(Version old, Version new)


Datenstrukturen


ValidationErrorInfo
	String error
	String occursInVersion
	boolean consider
	String resolvedInVersion
	
	

for each Revision in History
	Set errors = validate(Revision);
	Set infos = extractInfos(errors);



FUNCTION extractInfos(Set errors){
	Set result = EmptySet;	
	for each Error in errors
		ValidationErrorInfo info = extractInfo(Error)
		if (info.consider)
			result.add(info);
}
			

FUNCTION extractInfo(Error){
	
}
	
		
	
